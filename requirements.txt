# Runtime dependencies only. This file is what Hugging Face Spaces builds
# from (free CPU tier) — kept lean on purpose since a smaller dependency
# set means faster builds and faster cold starts. Test-only dependencies
# live in requirements-dev.txt instead.

# --- Core ML / CV (open-source only, no paid APIs) ---
torch==2.4.1
torchvision==0.19.1
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
opencv-python-headless>=4.9
Pillow>=10.2

# --- Explainability: Grad-CAM implemented manually in src/explainability/
# (FusionModel's dict-input signature doesn't fit off-the-shelf Grad-CAM
# libraries, which assume a single-tensor model input) — no extra dep needed

# --- Metadata / EXIF: uses PIL.ExifTags (already a Pillow dependency above),
# no separate exifread package needed

# --- Serving ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
streamlit>=1.32

# --- Experiment tracking / plotting ---
matplotlib>=3.8
pandas>=2.2
seaborn>=0.13
tqdm>=4.66

# --- Config management ---
pyyaml>=6.0
pydantic>=2.6

# --- Reproducibility ---
python-dotenv>=1.0
