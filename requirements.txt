# --- Core ML / CV (open-source only, no paid APIs) ---
torch==2.4.1
torchvision==0.19.1
numpy>=1.26
scipy>=1.11
scikit-learn>=1.4
opencv-python-headless>=4.9
Pillow>=10.2

# --- Explainability: Grad-CAM implemented manually in src/explainability/
# (FusionModel's dict-input signature doesn't fit off-the-shelf Grad-CAM
# libraries, which assume a single-tensor model input) — no extra dep needed

# --- Metadata / EXIF ---
exifread>=3.0.0

# --- Serving ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
streamlit>=1.32

# --- Experiment tracking / plotting ---
matplotlib>=3.8
pandas>=2.2
seaborn>=0.13
tqdm>=4.66

# --- Config management ---
pyyaml>=6.0
pydantic>=2.6

# --- Testing ---
pytest>=8.0
pytest-cov>=5.0     # optional: `pytest --cov=src --cov=api --cov-report=term-missing`
httpx>=0.27          # for testing FastAPI endpoints
piexif>=1.1.3        # test-only: constructs synthetic EXIF blocks in tests/test_exif.py

# --- Reproducibility ---
python-dotenv>=1.0
