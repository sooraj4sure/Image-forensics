# Dev/test-only dependencies — install ON TOP OF requirements.txt for local
# development. NOT installed on Hugging Face Spaces (keeps the deployed
# app's build lean — see requirements.txt).
#
# Install both with:
#   ./.venv/bin/pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
pytest-cov>=5.0     # `pytest --cov=src --cov=api --cov-report=term-missing`
httpx>=0.27          # for testing FastAPI endpoints
piexif>=1.1.3        # constructs synthetic EXIF blocks in tests/test_exif.py
